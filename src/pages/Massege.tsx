import { Badge, Box } from '@chakra-ui/react'
import React,{ReactElement} from 'react'
import Sidebar from '../component/sidebar';

const Massege = ():ReactElement => {
  return (
  <Box bg={"Purple"}>
      <Sidebar/>
      </Box>
  )
}

export default Massege;
